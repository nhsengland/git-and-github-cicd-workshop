{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Git and GitHub CI/CD Workshop","text":"<p>Welcome to the Git and GitHub CI/CD workshop, run by the NHS England Data Science and Applied AI team.</p> <p>This workshop is a dive into the automated features of Git and GitHub, specifically focusing on CI/CD workflows for Data Science projects. We will cover the mechanics and practical implementation of pre-commit hooks and GitHub Actions \u2014 two tools that automate quality checks, protect your repository from common mistakes, and keep your project standards consistent across a whole team.</p> <p>Pre-requisite Knowledge</p> Pre-requisite Description Python Knowledge of how to write and run Python code Git Basic command line usage \u2014 staging, committing, pushing GitHub Familiarity with repositories, Codespaces, and forking"},{"location":"#workshop-activities","title":"Workshop Activities","text":"<p>There are two activities. Pick one to complete during the live session and work through the other at your own pace.</p> <ul> <li> <p>Pre-Commit Hooks</p> <p>Set up local automation that runs before every commit. You will configure hooks for secret detection (<code>gitleaks</code>), code linting and formatting (<code>ruff</code>), and notebook output stripping (<code>nbstripout</code>).</p> </li> <li> <p>GitHub Actions</p> <p>Write CI/CD workflows that run automatically on GitHub. You will build a quality checks pipeline (<code>ruff</code> + <code>pytest</code>) and a documentation deployment workflow.</p> </li> </ul>"},{"location":"#getting-started","title":"Getting Started","text":"<p>Start with Getting Started to fork the repository and set up your environment.</p>"},{"location":"getting_started/","title":"Getting Started","text":"<p>Before starting the workshop activities, you need to get your environment set up. This should take about 10 minutes.</p> <p>By the end of this page you will have</p> <ul> <li> Forked this repository to your own GitHub account</li> <li> Opened it in GitHub Codespaces (or cloned it locally)</li> <li> Verified that <code>uv</code> and the project dependencies are installed</li> <li> Confirmed the MkDocs site serves correctly</li> </ul>"},{"location":"getting_started/#step-1-fork-the-repository","title":"Step 1: Fork the Repository","text":"<ol> <li>Navigate to the Git and GitHub CI/CD Workshop repository.</li> <li>Click the Fork button in the top-right corner.</li> <li>Click Create fork to confirm.</li> </ol> <p>Why fork?</p> <p>Forking creates your own copy of the repository under your GitHub account. This means you can push commits, trigger GitHub Actions, and publish GitHub Pages \u2014 all without affecting the original repository.</p>"},{"location":"getting_started/#step-2-open-in-github-codespaces","title":"Step 2: Open in GitHub Codespaces","text":"<ol> <li>In your forked repository, click the green Code button.</li> <li>Select the Codespaces tab.</li> <li>Click Create codespace on main.</li> </ol> <p>The Codespace will take a minute or two to start. It automatically runs <code>uv sync --all-groups</code> to install all project dependencies, including the <code>dev</code> and <code>docs</code> groups.</p> <p>Working locally instead?</p> <p>If you prefer to work on your own machine rather than Codespaces, clone your fork and run:</p> <pre><code># Install uv if you do not already have it\ncurl -LsSf https://astral.sh/uv/install.sh | sh\n\n# Install all project dependencies\nuv sync --all-groups\n</code></pre> <p>See the uv documentation for alternative installation methods.</p>"},{"location":"getting_started/#step-3-verify-the-environment","title":"Step 3: Verify the Environment","text":"<p>Once the Codespace is ready, open a terminal (Terminal \u2192 New Terminal) and check everything is installed:</p> <pre><code>uv run python --version\nuv run pytest --version\nuv run ruff --version\n</code></pre> <p>You should see version numbers printed for each. If any command fails, run <code>uv sync --all-groups</code> to reinstall.</p>"},{"location":"getting_started/#step-4-serve-the-workshop-site","title":"Step 4: Serve the Workshop Site","text":"<p>Start a live preview of this documentation site:</p> <pre><code>uv run mkdocs serve --livereload\n</code></pre> <p>Once running, a notification will appear in VS Code / Codespaces offering to open the forwarded port. Click it to open the site in your browser.</p> <p>Live reload</p> <p>The server watches your <code>docs/</code> folder for changes. Every time you save a Markdown file, the browser tab refreshes automatically \u2014 no manual reload needed. Try it: edit a line in any <code>.md</code> file, save, and watch the browser update within a second or two.</p> <p>Stopping the server</p> <p>Press <code>Ctrl + C</code> in the terminal to stop the MkDocs server.</p>"},{"location":"getting_started/#youre-ready","title":"You're Ready","text":"<p>Head to the workshops section and pick your activity.</p>"},{"location":"workshop_presentation/","title":"Git and GitHub CI/CD Workshop","text":"<p>Welcome to the Git and GitHub CI/CD workshop! This workshop is a hands-on dive into the automated features of Git and GitHub, focusing on the practical CI/CD tools that Data Science teams use to keep code quality high and collaboration smooth.</p>"},{"location":"workshop_presentation/github_actions_intro/","title":"GitHub Actions","text":""},{"location":"workshop_presentation/github_actions_intro/#what-is-github-actions","title":"What is GitHub Actions?","text":"<p>Automation that runs in the cloud, triggered by events in your repository.</p> <ul> <li> <p> Runs on GitHub's servers</p> <p>No local setup needed. Every contributor gets the same environment.</p> </li> <li> <p> Triggered by events</p> <p>Push a commit, open a pull request, merge to <code>main</code> \u2014 Actions reacts automatically.</p> </li> <li> <p> The server-side safety net</p> <p>Pre-commit hooks can be skipped locally. GitHub Actions can't be bypassed.</p> </li> <li> <p> Reusable building blocks</p> <p>Use community-built Actions from the GitHub Marketplace \u2014 no reinventing the wheel.</p> </li> </ul>"},{"location":"workshop_presentation/github_actions_intro/#how-does-it-work","title":"How does it work?","text":""},{"location":"workshop_presentation/github_actions_intro/#the-flow","title":"The flow","text":"<ol> <li>You push code to GitHub</li> <li>GitHub reads your workflow files</li> <li>A runner (a fresh virtual machine) spins up</li> <li>Your steps execute in order</li> <li> All pass \u2192 green tick on your PR</li> <li> Any fail \u2192 red cross, merge is blocked</li> </ol>"},{"location":"workshop_presentation/github_actions_intro/#the-workflow-file","title":"The workflow file","text":"<p>One YAML file defines everything:</p> .github/workflows/tests.yml<pre><code>name: Run tests\n\non: [push, pull_request]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: astral-sh/setup-uv@v5\n      - run: uv run pytest tests/\n</code></pre> <ul> <li>Stored in <code>.github/workflows/</code></li> <li><code>on:</code> controls when it triggers</li> <li><code>steps:</code> are the commands to run</li> </ul>"},{"location":"workshop_presentation/github_actions_intro/#pre-commit-hooks-vs-github-actions","title":"Pre-commit hooks vs GitHub Actions","text":"<ul> <li> <p> Pre-commit hooks</p> <p>Fast, local, immediate feedback. Runs before you can even push.</p> </li> <li> <p> GitHub Actions</p> <p>Server-side, can't be skipped, runs in a clean environment every time.</p> </li> </ul> <p>Use both</p> <p>Hooks give you speed. Actions give you the guarantee. Together they cover every angle.</p>"},{"location":"workshop_presentation/open_up_the_repository/","title":"Let's Get Set Up","text":"<ul> <li> <p> Open this website</p> <p>nhsengland.github.io/git-and-github-cicd-workshop/</p> </li> <li> <p> Follow Getting Started</p> <p>Getting Started \u2014 fork the repo and open it in Codespaces.</p> </li> <li> <p> Start the live docs site</p> <p>Run the command below and open the forwarded port in your browser.</p> </li> <li> <p> Edit and watch it update</p> <p>Save any Markdown file \u2014 the site refreshes automatically.</p> </li> </ul> <pre><code>uv run mkdocs serve --livereload\n</code></pre> <p>Tip</p> <p>Keep this site and your Codespace open side by side throughout the workshop.</p>"},{"location":"workshop_presentation/opinions_and_disclaimers/","title":"Opinions and Disclaimers","text":""},{"location":"workshop_presentation/opinions_and_disclaimers/#opinions","title":"Opinions","text":"<ul> <li> <p> <code>uv</code> is the right tool for Python environments</p> <p>Fast, clean dependency management. The <code>astral-sh/setup-uv</code> Action means your local environment and CI match exactly \u2014 no surprises.</p> </li> <li> <p> Pre-commit hooks and GitHub Actions complement each other</p> <p>Hooks = fast local feedback before you push. Actions = server-side guarantee that can't be bypassed. You want both.</p> </li> </ul>"},{"location":"workshop_presentation/opinions_and_disclaimers/#disclaimers","title":"Disclaimers","text":"<ul> <li> <p> Python-focused</p> <p>All live demos use Python. R equivalents are noted in the workshop pages, but hands-on activities are Python-first.</p> </li> <li> <p> Built with AI assistance</p> <p>I used AI tools to help draft and structure this content. I've reviewed every section and worked through the steps myself \u2014 but things may have been missed. Raise an issue on GitHub if you spot something.</p> </li> <li> <p> Real projects are messier</p> <p>These steps are designed to work cleanly in a Codespace. On real projects with legacy code or restricted networks, the path may be bumpier. The principles are the same.</p> </li> </ul>"},{"location":"workshop_presentation/pre_commits/","title":"Pre-Commit Hooks","text":""},{"location":"workshop_presentation/pre_commits/#what-is-a-pre-commit-hook","title":"What is a pre-commit hook?","text":"<p>A small script that runs automatically before every <code>git commit</code>.</p> <ul> <li> <p> Catches problems early</p> <p>Before code ever reaches GitHub \u2014 on your machine, every time.</p> </li> <li> <p> Shared across the team</p> <p>The config lives in the repo. Everyone gets the same checks automatically.</p> </li> <li> <p> Can fix things for you</p> <p>Many hooks (like formatters) just fix the issue silently. Others block the commit and tell you what's wrong.</p> </li> <li> <p> Can't be skipped by accident</p> <p>If a hook fails, the commit doesn't happen. The bar is raised for everyone.</p> </li> </ul>"},{"location":"workshop_presentation/pre_commits/#how-does-it-work","title":"How does it work?","text":"<p>Hooks we'll use today</p> <ul> <li>gitleaks \u2014 blocks accidentally committed passwords or API keys</li> <li>ruff \u2014 automatically fixes Python linting violations</li> <li>ruff-format \u2014 automatically formats Python code</li> <li>nbstripout \u2014 strips outputs from Jupyter notebooks before committing</li> </ul>"},{"location":"workshop_presentation/pre_commits/#the-flow","title":"The flow","text":"<ol> <li>You run <code>git commit</code></li> <li>Pre-commit intercepts it</li> <li>Each hook runs in order</li> <li> All pass \u2192 commit goes through</li> <li> Any fail \u2192 commit is blocked, fix and retry</li> </ol>"},{"location":"workshop_presentation/pre_commits/#the-config-file","title":"The config file","text":"<p>One file in your repo controls everything:</p> .pre-commit-config.yaml<pre><code>repos:\n  - repo: https://github.com/astral-sh/ruff-pre-commit\n    rev: v0.4.0\n    hooks:\n      - id: ruff         # linting\n      - id: ruff-format  # formatting\n</code></pre> <ul> <li>Hooks are pulled from public repos</li> <li>Pin the version with <code>rev:</code></li> <li>Run <code>pre-commit install</code> once to activate</li> </ul>"},{"location":"workshop_presentation/who_am_i/","title":"Who am I?","text":""},{"location":"workshop_presentation/who_am_i/#joe-wilson","title":"Joe Wilson","text":""},{"location":"workshop_presentation/who_am_i/#senior-data-scientist-nhs-england-data-science-and-applied-ai-team","title":"Senior Data Scientist | NHS England Data Science and Applied AI Team","text":""},{"location":"workshop_presentation/who_am_i/#at-work","title":"At Work","text":"<ul> <li>Background in Aerospace Engineering and Data Science</li> <li>Code mainly in Python, but also R and SQL (and LabVIEW from engineering days!)</li> <li>Working in the Reproducible Analytical Pipelines (RAP) space for four years at NHS England</li> <li>Currently working on:<ul> <li>Predicting Length of Stay in Hospital</li> <li>Automating metric production in FDP</li> </ul> </li> <li>NHS R Fellow and sit on the NHS Open Analytics Committee</li> <li>Tech Lead for Gold RAP Delivery</li> <li>Mission: To make high-standard, best-practice pipelines the easy, default behaviour for everyone</li> </ul>"},{"location":"workshop_presentation/who_am_i/#at-home","title":"At Home","text":"<ul> <li>Live in Leeds with my wife</li> <li>Love getting outside \u2014 running and hiking! <ul> <li>Ran the Manchester Marathon two weeks ago</li> <li>Heading off to the Lake District for four days of hiking </li> </ul> </li> <li>General nerd with a large board game collection <ul> <li>Current favourite: Skyjo</li> </ul> </li> </ul>"},{"location":"workshop_presentation/who_am_i/#find-me-online","title":"Find Me Online","text":"<ul> <li> <p> GitHub</p> <p>@josephwilson8-nhs</p> </li> <li> <p> LinkedIn</p> <p>Joseph Wilson</p> </li> <li> <p> Email</p> <p>joseph.wilson8@nhs.net</p> </li> <li> <p> Teams</p> <p>Chat with me on Teams</p> </li> </ul>"},{"location":"workshop_presentation/workshop_context/","title":"Why does any of this matter?","text":""},{"location":"workshop_presentation/workshop_context/#automation-is-what-makes-quality-sustainable","title":"Automation is what makes quality sustainable","text":"<ul> <li> <p> Writing good code once is easy</p> <p>Maintaining that standard across a whole team, over months and years, is hard.</p> </li> <li> <p> Manual review has limits</p> <p>It's inconsistent, time-consuming, and dependent on the reviewer having a good day.</p> </li> <li> <p> Automation doesn't have bad days</p> <p>If the tooling fixes formatting, flags secrets, and runs tests on every change \u2014 quality becomes the default.</p> </li> </ul> <p>The idea in one sentence</p> <p>Make the right thing the easy thing.</p>"},{"location":"workshop_presentation/workshop_context/#this-matters-for-rap","title":"This matters for RAP","text":"<p>Automated quality checks are a direct enabler of Silver RAP and Gold RAP.</p> <ul> <li>A pipeline that passes CI on every merge is a pipeline you can trust</li> <li>Build the habit early \u2014 consistent quality should be the lazy default, not an extra effort</li> </ul>"},{"location":"workshops/","title":"Workshops","text":"<p>There are two workshop activities. Pick one to work through during the live session \u2014 you can come back and do the other in your own time.</p> <p>Not sure which to pick?</p> <p>If you are more interested in local tooling and developer workflow, start with Pre-Commit Hooks.</p> <p>If you are more interested in cloud automation and GitHub integrations, start with GitHub Actions.</p> <p>Both activities use the same flawed repository as their starting point, so they work independently.</p>"},{"location":"workshops/#pre-commit-hooks","title":"Pre-Commit Hooks","text":"<p>Configure automation that runs on your local machine before every <code>git commit</code>. You will set up three hooks:</p> <ul> <li>gitleaks \u2014 scans for hardcoded secrets and credentials</li> <li>ruff \u2014 checks and auto-fixes code formatting and linting violations</li> <li>nbstripout \u2014 strips Jupyter notebook outputs before they are committed</li> </ul> <p>The repository already has intentional problems baked in \u2014 you will watch the hooks catch them.</p>"},{"location":"workshops/#github-actions","title":"GitHub Actions","text":"<p>Write CI/CD workflows that run automatically on GitHub's servers on every push and pull request. You will build:</p> <ul> <li>A quality checks workflow \u2014 runs <code>ruff</code> and <code>pytest</code></li> <li>A documentation deployment workflow \u2014 builds and publishes this MkDocs site to GitHub Pages</li> </ul> <p>Getting Help</p> <p>During the workshops, please ask questions \u2014 do not hesitate to flag if something is not working.</p> <p>Outside of the workshop, open an issue if you find a problem with the materials.</p>"},{"location":"workshops/github_actions/","title":"GitHub Actions","text":"<p>Learning Objectives</p> <p>By the end of this activity, you will be able to:</p> <ul> <li> Describe what GitHub Actions is and how workflows are structured</li> <li> Write a workflow that runs code quality checks on every push and pull request</li> <li> Write a workflow that deploys documentation to GitHub Pages</li> <li> Configure Python and install dependencies in CI</li> </ul>"},{"location":"workshops/github_actions/#what-is-github-actions","title":"What is GitHub Actions?","text":"<p>GitHub Actions is GitHub's built-in CI/CD platform. You define workflows as YAML files stored in <code>.github/workflows/</code>. GitHub automatically runs these workflows when specified events occur in your repository \u2014 such as a push, a pull request, or a scheduled time.</p> <p>Each workflow runs inside a fresh virtual machine (runner) provisioned by GitHub. This means every run starts from a clean slate, making your checks reproducible and consistent.</p> <p>A workflow has this basic structure:</p> <pre><code>name: My Workflow            # (1)!\n\non:                          # (2)!\n  push:\n  pull_request:\n\njobs:                        # (3)!\n  my-job:\n    runs-on: ubuntu-latest   # (4)!\n    steps:                   # (5)!\n      - uses: actions/checkout@v4\n      - run: echo \"Hello, CI!\"\n</code></pre> <ol> <li>A human-readable name shown in the Actions tab on GitHub.</li> <li>Triggers \u2014 the events that cause the workflow to run. <code>push</code> fires on every push to any branch; <code>pull_request</code> fires when a PR is opened or updated.</li> <li>Jobs \u2014 groups of steps. Multiple jobs run in parallel by default; add <code>needs:</code> to sequence them.</li> <li>The runner image. <code>ubuntu-latest</code> is the most common choice for Python projects.</li> <li>Steps \u2014 the individual tasks within a job. <code>uses:</code> runs a pre-built Action from the GitHub Marketplace; <code>run:</code> executes a shell command.</li> </ol>"},{"location":"workshops/github_actions/#why-automate-with-github-actions-in-data-science","title":"Why Automate with GitHub Actions in Data Science?","text":"<p>Pre-commit hooks and GitHub Actions solve related but different problems:</p> Pre-Commit Hooks GitHub Actions Where it runs Your local machine GitHub's servers When it runs Before every local commit On every push / pull request Who controls it Each developer installs it Enforced for the whole repository Can auto-fix files Yes No \u2014 reports failures only Can block merges No Yes \u2014 via branch protection rules <p>Use both together: hooks give fast local feedback, and Actions enforce the same checks for every developer on every push.</p> <p>R Equivalents</p> <p>GitHub Actions is language-agnostic. For R projects:</p> PythonR <p>Use <code>actions/setup-python</code> to install Python, then <code>pip install</code> to install dependencies.</p> <pre><code>- name: Set up Python\n  uses: actions/setup-python@v5\n  with:\n    python-version: \"3.12\"\n\n- name: Install dependencies\n  run: pip install -e \".[dev]\"\n</code></pre> <p>Use <code>r-lib/actions</code> for a standardised R setup. The <code>pak</code> package manager handles fast dependency installation.</p> <pre><code>- uses: r-lib/actions/setup-r@v2\n  with:\n    r-version: '4.4'\n\n- uses: r-lib/actions/install-r-dependencies@v2\n  with:\n    cache-version: 1\n\n- name: Run tests\n  run: Rscript -e 'testthat::test_dir(\"tests/testthat\")'\n\n- name: Lint\n  run: Rscript -e 'lintr::lint_dir(\"R\")'\n</code></pre>"},{"location":"workshops/github_actions/#activity-build-two-cicd-workflows","title":"Activity: Build Two CI/CD Workflows","text":""},{"location":"workshops/github_actions/#task-1-create-the-workflows-directory","title":"Task 1: Create the Workflows Directory","text":"<p>GitHub Actions looks for workflow files in <code>.github/workflows/</code>. Create the directory:</p> <pre><code>mkdir -p .github/workflows\n</code></pre>"},{"location":"workshops/github_actions/#task-2-give-the-workflow-a-name-and-triggers","title":"Task 2: Give the Workflow a Name and Triggers","text":"<p>Create a new file <code>.github/workflows/quality-checks.yml</code>. Start with just the name and the events that should trigger it:</p> <pre><code>name: Quality Checks\n\non:\n  push:\n  pull_request:\n</code></pre> <p>What are triggers?</p> <p>The <code>on:</code> block controls when the workflow runs.</p> <ul> <li><code>push</code> fires on every push to any branch.</li> <li><code>pull_request</code> fires when a pull request is opened or updated.</li> </ul> <p>Together these mean: run quality checks whenever any code change arrives, whether directly pushed or proposed via PR. You can narrow them down with <code>branches:</code> filters once you are comfortable with the basics.</p>"},{"location":"workshops/github_actions/#task-3-add-a-job-and-choose-a-runner","title":"Task 3: Add a Job and Choose a Runner","text":"<p>Add the <code>jobs:</code> block below your triggers:</p> <pre><code>jobs:\n  quality:\n    runs-on: ubuntu-latest\n</code></pre> <p>Jobs and runners</p> <p>A job is a group of steps that run together. Multiple jobs in the same workflow run in parallel by default \u2014 add <code>needs:</code> to sequence them.</p> <p><code>runs-on: ubuntu-latest</code> tells GitHub to spin up a fresh Ubuntu virtual machine for each run. The machine starts completely empty; every run must install its own dependencies from scratch. This is what makes CI reproducible.</p>"},{"location":"workshops/github_actions/#task-4-check-out-the-code","title":"Task 4: Check Out the Code","text":"<p>Add a <code>steps:</code> key under your job, with the checkout step as its first entry:</p> <pre><code>    steps:\n      - name: Checkout code\n        uses: actions/checkout@v4\n</code></pre> <p><code>uses:</code> vs <code>run:</code></p> <p>Steps can do one of two things:</p> <ul> <li><code>uses:</code> runs a pre-built Action from the GitHub Marketplace. <code>actions/checkout@v4</code> is maintained by GitHub and handles the full <code>git clone</code> for you.</li> <li><code>run:</code> executes a shell command directly on the runner, just like typing it in a terminal.</li> </ul> <p>The <code>@v4</code> suffix pins the Action to a specific major version, preventing unexpected changes from breaking your workflow.</p>"},{"location":"workshops/github_actions/#task-5-install-python-and-dependencies","title":"Task 5: Install Python and Dependencies","text":"<p>Append two more steps directly below the checkout step:</p> <pre><code>      - name: Set up Python\n        uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - name: Install dependencies\n        run: pip install -e \".[dev]\"\n</code></pre> <p>Why install dependencies every run?</p> <p>The runner is a fresh VM \u2014 it has no packages installed. <code>pip install -e \".[dev]\"</code> installs the project in editable mode along with everything in the <code>dev</code> dependency group, which includes <code>pytest</code> and <code>ruff</code>.</p> <p>The <code>with:</code> block passes inputs to an Action. Here it tells <code>setup-python</code> exactly which Python version to install, keeping every run consistent.</p>"},{"location":"workshops/github_actions/#task-6-add-the-quality-check-steps","title":"Task 6: Add the Quality Check Steps","text":"<p>Append the final two steps:</p> <pre><code>      - name: Lint and format check\n        run: ruff check .\n\n      - name: Run tests\n        run: pytest tests/\n</code></pre> <p><code>ruff check .</code> exits with a non-zero code if there are any linting violations, which fails the step and the whole job. <code>pytest tests/</code> does the same if any test fails.</p> <p>This workflow will fail on the first push \u2014 deliberately</p> <p>The repository contains a logic bug in <code>practice_level_gp_appointments/analytics.py</code> that causes two test failures. Pre-commit hooks catch secrets and formatting violations locally, but they cannot run your test suite \u2014 that is what GitHub Actions is for.</p> <p>You will see the <code>pytest</code> step fail in the Actions tab. The Bonus Activity at the end of this workshop walks through tracking down and fixing the bug.</p>"},{"location":"workshops/github_actions/#task-7-push-and-watch-it-run","title":"Task 7: Push and Watch it Run","text":"<p>Stage and push the new workflow file:</p> <pre><code>git add .github/workflows/quality-checks.yml\ngit commit -m \"add quality checks workflow\"\ngit push\n</code></pre> <p>Navigate to your forked repository on GitHub and click the Actions tab. You should see the workflow running (or already completed). Click into it to see the logs from each step.</p> <p>Protecting your main branch</p> <p>Once your workflow is passing, go to Settings \u2192 Branches \u2192 Add branch protection rule on your fork. Enable Require status checks to pass before merging and select <code>Quality Checks</code>. This prevents any pull request from being merged until CI passes \u2014 the same pattern used on production repositories.</p>"},{"location":"workshops/github_actions/#task-8-name-and-trigger-the-deploy-workflow","title":"Task 8: Name and Trigger the Deploy Workflow","text":"<p>Create a second file <code>.github/workflows/deploy-docs.yml</code>. This workflow has a different purpose \u2014 it deploys documentation \u2014 so it needs different triggers:</p> <pre><code>name: Deploy Documentation\n\non:\n  push:\n    branches:\n      - main\n</code></pre> <p>Narrowing triggers with <code>branches:</code></p> <p>Unlike the quality checks workflow that fires on every push, documentation should only deploy when code reaches <code>main</code>. Deploying from feature branches would overwrite the live site with unreviewed work.</p> <p>The <code>branches:</code> filter restricts the <code>push</code> trigger to only that branch.</p>"},{"location":"workshops/github_actions/#task-9-add-permissions-and-the-deploy-job","title":"Task 9: Add Permissions and the Deploy Job","text":"<p>This workflow needs write access to push the built site to the <code>gh-pages</code> branch. Add the <code>permissions:</code> block and the job below your triggers:</p> <pre><code>permissions:\n  contents: write\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n</code></pre> <p>Why declare permissions?</p> <p>By default, GitHub Actions workflows have read-only access to the repository. <code>contents: write</code> grants the job permission to push commits \u2014 which <code>mkdocs gh-deploy</code> needs in order to update the <code>gh-pages</code> branch.</p> <p>Declaring only the permissions you need is good security practice: it limits the blast radius if a third-party Action in the job were ever compromised.</p>"},{"location":"workshops/github_actions/#task-10-add-the-steps","title":"Task 10: Add the Steps","text":"<p>Complete the workflow by adding <code>steps:</code> under the job:</p> <pre><code>    steps:\n      - name: Checkout code\n        uses: actions/checkout@v4\n\n      - name: Set up Python\n        uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - name: Install docs dependencies\n        run: pip install -e \".[docs]\"\n\n      - name: Deploy to GitHub Pages\n        run: mkdocs gh-deploy --force\n</code></pre> <p>Notice <code>pip install -e \".[docs]\"</code> instead of <code>.[dev]</code> \u2014 there is no reason to install <code>pytest</code> or <code>ruff</code> for a documentation build. Keeping dependency groups separate makes CI faster and the intent clearer.</p> <p><code>mkdocs gh-deploy --force</code> builds the static site and force-pushes it to the <code>gh-pages</code> branch. GitHub Pages serves this branch automatically.</p> <p>Enabling GitHub Pages on your fork</p> <p>For the deployment to work, go to Settings \u2192 Pages in your forked repository. Set the source to Deploy from a branch and select <code>gh-pages</code>. After the workflow runs for the first time, your site will be live at:</p> <pre><code>https://\\&lt;your-username\\&gt;.github.io/git-and-github-cicd-workshop/\n</code></pre>"},{"location":"workshops/github_actions/#task-11-push-and-verify-the-deployment","title":"Task 11: Push and Verify the Deployment","text":"<pre><code>git add .github/workflows/deploy-docs.yml\ngit commit -m \"add docs deploy workflow\"\ngit push\n</code></pre> <p>Once the workflow completes, your documentation site will be live. Check the Actions tab to confirm the deployment step succeeded, then open your GitHub Pages URL to see the site.</p> <p>Well done!</p> <p>You now have two automated workflows:</p> <ul> <li>Quality Checks \u2014 ruff and pytest run on every push and pull request, catching issues before they reach <code>main</code></li> <li>Deploy Documentation \u2014 your MkDocs site is built and published automatically whenever code is merged</li> </ul> <p>If you haven't already, try the Pre-Commit Hooks workshop to catch secrets and formatting violations before they ever reach CI.</p>"},{"location":"workshops/github_actions/#complete-files-reference","title":"Complete Files Reference","text":"<p>If anything looks wrong in your workflow files, compare against the complete versions here.</p> <code>.github/workflows/quality-checks.yml</code> \u2014 complete file <pre><code>name: Quality Checks\n\non:\n  push:\n  pull_request:\n\njobs:\n  quality:\n    runs-on: ubuntu-latest\n\n    steps:\n      - name: Checkout code\n        uses: actions/checkout@v4\n\n      - name: Set up Python\n        uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - name: Install dependencies\n        run: pip install -e \".[dev]\"\n\n      - name: Lint and format check\n        run: ruff check .\n\n      - name: Run tests\n        run: pytest tests/\n</code></pre> <code>.github/workflows/deploy-docs.yml</code> \u2014 complete file <pre><code>name: Deploy Documentation\n\non:\n  push:\n    branches:\n      - main\n\npermissions:\n  contents: write\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n\n    steps:\n      - name: Checkout code\n        uses: actions/checkout@v4\n\n      - name: Set up Python\n        uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - name: Install docs dependencies\n        run: pip install -e \".[docs]\"\n\n      - name: Deploy to GitHub Pages\n        run: mkdocs gh-deploy --force\n</code></pre>"},{"location":"workshops/github_actions/#bonus-activity-fix-the-failing-tests","title":"Bonus Activity: Fix the Failing Tests","text":"<p>The <code>quality</code> job is failing because <code>analytics.py</code> contains a subtle logic bug. Open the failing test run in the Actions tab and read the assertion error to understand what is going wrong.</p> <p>The bug is in <code>practice_level_gp_appointments/analytics.py</code>. Look at how <code>total_appointments</code> is calculated \u2014 it is counting the number of rows in the DataFrame rather than the total number of appointments.</p> Reveal the fix <p>Find this line:</p> <pre><code>total_appointments = len(df)\n</code></pre> <p>Replace it with:</p> <pre><code>total_appointments = df[\"count_of_appointments\"].sum()\n</code></pre> <p>Run <code>uv run pytest tests/unittests/test_analytics.py</code> locally to confirm both tests pass, then push. Watch the workflow go green.</p>"},{"location":"workshops/github_actions/#going-further","title":"Going Further","text":"Workflow badges <p>You can add a status badge to your <code>README.md</code> to show whether your CI is passing. On the Actions tab, click your workflow name, then click the three-dot menu and select Create status badge.</p> <pre><code>![Quality Checks](https://github.com/&lt;org&gt;/&lt;repo&gt;/actions/workflows/quality-checks.yml/badge.svg)\n</code></pre> Secrets in GitHub Actions <p>Never hardcode credentials in your workflow files. Store them in Settings \u2192 Secrets and variables \u2192 Actions and reference them as environment variables:</p> <pre><code>- name: Deploy\n  env:\n    MY_API_KEY: ${{ secrets.MY_API_KEY }}\n  run: python deploy.py\n</code></pre> Caching dependencies <p>You can cache your <code>pip</code> dependencies to speed up subsequent workflow runs. Add the <code>cache</code> input to <code>actions/setup-python</code>:</p> <pre><code>- name: Set up Python\n  uses: actions/setup-python@v5\n  with:\n    python-version: \"3.12\"\n    cache: \"pip\"\n</code></pre> <p>This caches the pip download cache between runs, meaning dependencies install much faster after the first run.</p>"},{"location":"workshops/pre-commit-hooks/","title":"Pre-Commit Hooks","text":"<p>Learning Objectives</p> <p>By the end of this activity, you will be able to:</p> <ul> <li> Explain what pre-commit hooks are and why they matter in Data Science</li> <li> Configure hooks for secret detection, linting, and notebook output stripping</li> <li> Install and run the <code>pre-commit</code> framework using <code>uv</code></li> <li> Understand the difference between hooks that auto-fix files and hooks that block commits</li> </ul>"},{"location":"workshops/pre-commit-hooks/#what-are-pre-commit-hooks","title":"What are Pre-Commit Hooks?","text":"<p>A pre-commit hook is a script that runs automatically every time you run <code>git commit</code>, before the commit is actually created. If the hook fails, the commit is blocked.</p> <p>The <code>pre-commit</code> framework manages hooks as shareable configuration. Instead of each developer writing their own scripts, you define all your hooks in a single <code>.pre-commit-config.yaml</code> file that lives in the repository. Everyone gets the same checks.</p> <pre><code>git commit -m \"my changes\"\n        \u2502\n        \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  pre-commit runs  \u2502  \u2190 hooks fire here, before the commit exists\n\u2502  your hooks       \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n        \u2502\n   \u250c\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2510\n   \u2502 PASS?   \u2502\n   \u2514\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2518\n        \u2502\n   \u2713 Yes \u2192 commit is created\n   \u2717 No  \u2192 commit is blocked (files may be auto-fixed for you)\n</code></pre>"},{"location":"workshops/pre-commit-hooks/#why-use-pre-commit-hooks-in-data-science","title":"Why Use Pre-Commit Hooks in Data Science?","text":"<p>Three problems hooks solve</p> Leaked SecretsMessy CodeDirty Notebooks <p>Hardcoded passwords, API keys, and database credentials accidentally committed to Git are one of the most common security incidents in software teams. Once pushed to a remote repository, a secret is effectively compromised \u2014 even if you delete it in a later commit, it remains in the Git history.</p> <p>gitleaks scans your staged files for patterns that look like credentials before they can reach Git history.</p> <p>Inconsistent formatting and style makes code harder to review and maintain. Fixing it manually is tedious and easy to forget.</p> <p>ruff checks and automatically fixes formatting and linting violations on every commit.</p> <p>Jupyter notebooks store cell outputs \u2014 plots, tables, printed values \u2014 inside the <code>.ipynb</code> file as JSON. Committing these outputs bloats the repository, makes diffs near-unreadable, and can accidentally publish sensitive data.</p> <p>nbstripout automatically strips all outputs from notebooks before they are committed.</p> <p>R Equivalents</p> <p>The <code>pre-commit</code> framework is language-agnostic and works for R projects too.</p> <ul> <li>Secrets: the same <code>gitleaks</code> hook works for any language</li> <li>Formatting/Linting: the <code>precommit</code> R package provides hooks for <code>styler</code> (formatting) and <code>lintr</code> (linting). Install it with <code>usethis::use_precommit()</code>.</li> <li>Notebooks: <code>nbstripout</code> handles R-backed Jupyter notebooks. For R Markdown and Quarto files, <code>lintr</code> hooks can check code quality.</li> </ul>"},{"location":"workshops/pre-commit-hooks/#activity-implement-the-hooks","title":"Activity: Implement the Hooks","text":""},{"location":"workshops/pre-commit-hooks/#task-1-explore-whats-already-wrong","title":"Task 1: Explore What's Already Wrong","text":"<p>This repository has some intentional problems baked in. Let's find them first.</p> <p>Run the ruff linter to see the formatting issues:</p> <pre><code>uv run ruff check practice_level_gp_appointments/\n</code></pre> <p>You should see errors in <code>analytics.py</code>:</p> <ul> <li>E401 \u2014 multiple imports on one line (<code>import os, sys</code>)</li> <li>F401 \u2014 unused imports (<code>os</code> and <code>sys</code> are never used)</li> <li>I001 \u2014 imports are not sorted correctly</li> </ul> <p>Now open <code>practice_level_gp_appointments/config.py</code>. Notice the hardcoded AWS credentials near the top of the file.</p> <p>This is exactly how credential leaks happen</p> <p>These keys would be committed to Git, pushed to GitHub, and could be scraped by automated bots within minutes of being public. The <code>gitleaks</code> hook is specifically designed to catch this before it ever reaches version control.</p> <p>Finally, open <code>notebooks/analysis.ipynb</code>. Notice that the cells have execution counts and outputs stored inside them \u2014 exactly the kind of content that bloats repositories and makes diffs difficult to read.</p>"},{"location":"workshops/pre-commit-hooks/#task-2-create-the-config-file-and-add-secret-detection","title":"Task 2: Create the Config File and Add Secret Detection","text":"<p>Create a new file called <code>.pre-commit-config.yaml</code> in the root of the repository. Start with just the <code>gitleaks</code> hook:</p> <pre><code>repos:\n  - repo: https://github.com/gitleaks/gitleaks\n    rev: v8.18.4\n    hooks:\n      - id: gitleaks\n</code></pre> <p>How the config file works</p> <p>The <code>pre-commit</code> framework reads <code>.pre-commit-config.yaml</code> to know which hooks to run. Each entry under <code>repos:</code> points to an external Git repository that provides one or more hooks.</p> <p>The <code>rev</code> field pins the exact version to use \u2014 <code>pre-commit</code> downloads and caches it automatically. Pinning versions means your hooks behave identically for every developer on the team, regardless of when they cloned the repository.</p> <p>gitleaks scans staged files for patterns that match real credentials \u2014 AWS keys, tokens, passwords. It cannot auto-fix the problem; only you can remove a secret.</p>"},{"location":"workshops/pre-commit-hooks/#task-3-add-linting","title":"Task 3: Add Linting","text":"<p>Append the <code>ruff</code> entry to <code>.pre-commit-config.yaml</code>:</p> <pre><code>  - repo: https://github.com/astral-sh/ruff-pre-commit\n    rev: v0.4.0\n    hooks:\n      - id: ruff\n        args: [--fix]\n      - id: ruff-format\n</code></pre> <p>Two hooks from one repo</p> <p><code>ruff-pre-commit</code> provides two separate hooks:</p> <ul> <li><code>ruff</code> \u2014 linting. The <code>--fix</code> argument tells it to auto-correct any violations it knows how to fix, then exit with an error because it modified files (prompting you to review and re-stage them).</li> <li><code>ruff-format</code> \u2014 code formatting, equivalent to running <code>ruff format</code>. Formats to a consistent style automatically.</li> </ul> <p>Both hooks read your project's <code>pyproject.toml</code> for configuration, so they behave the same as running <code>ruff</code> locally.</p>"},{"location":"workshops/pre-commit-hooks/#task-4-add-notebook-output-stripping","title":"Task 4: Add Notebook Output Stripping","text":"<p>Append the final entry to <code>.pre-commit-config.yaml</code>:</p> <pre><code>  - repo: https://github.com/kynan/nbstripout\n    rev: 0.7.1\n    hooks:\n      - id: nbstripout\n</code></pre> <p>Why strip notebook outputs?</p> <p>Jupyter notebooks store cell outputs \u2014 plots, tables, printed values \u2014 as JSON inside the <code>.ipynb</code> file. Committing these outputs bloats the repository, produces near-unreadable diffs, and can accidentally publish sensitive query results or data previews.</p> <p>nbstripout strips all outputs and execution counts before the file is staged. It modifies the file in-place, so after it runs you will see the notebook as \"modified\" in <code>git diff</code> \u2014 which is exactly what you want.</p>"},{"location":"workshops/pre-commit-hooks/#task-5-install-the-hooks","title":"Task 5: Install the Hooks","text":"<p>Install the hooks into your local Git repository:</p> <pre><code>uv run pre-commit install\n</code></pre> <p>You should see:</p> <pre><code>pre-commit installed at .git/hooks/pre-commit\n</code></pre> <p>What does this do?</p> <p>It writes a small script to <code>.git/hooks/pre-commit</code>. Git calls this script automatically on every <code>git commit</code>. The hook tools themselves are downloaded and cached in <code>~/.cache/pre-commit/</code> the first time they run.</p> <p>Note that hooks are local \u2014 they live in <code>.git/</code>, which is never committed. Every developer who clones the repository needs to run <code>pre-commit install</code> themselves. This is a good thing to document in your <code>README.md</code>.</p>"},{"location":"workshops/pre-commit-hooks/#task-6-attempt-a-commit","title":"Task 6: Attempt a Commit","text":"<p>Stage all the changes made so far and attempt a commit:</p> <pre><code>git add .\ngit commit -m \"add analysis notebook and config\"\n</code></pre> <p>Watch what happens. You should see output similar to this:</p> <pre><code>gitleaks................................................................Failed\n- hook id: gitleaks\n- exit code: 1\n\nFinding:     AWS_SECRET_ACCESS_KEY = \"wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY\"\n  RuleID:    aws-secret-access-key\n  File:      practice_level_gp_appointments/config.py\n  Line:      19\n\nruff (run with --fix)...............................................Failed\n- hook id: ruff\n- exit code: 1\n- files were modified by this hook\n\nruff-format..........................................................Passed\nnbstripout...........................................................Passed\n</code></pre> <p>The commit was blocked \u2014 and that is the point</p> <p>Three hooks ran. Here is what each one did:</p> Hook Result Why <code>gitleaks</code> FAILED Found hardcoded AWS credentials in <code>config.py</code>. Cannot auto-fix \u2014 only you can remove a secret. <code>ruff</code> FAILED (but auto-fixed) Fixed the import issues in <code>analytics.py</code>, then exited with an error because it modified files. <code>ruff-format</code> Passed Formatting was already acceptable. <code>nbstripout</code> Passed Stripped outputs from the notebook. <p>Because the commit was blocked, nothing was committed. Run <code>git diff</code> to see the fixes <code>ruff</code> made automatically to <code>analytics.py</code>.</p>"},{"location":"workshops/pre-commit-hooks/#task-7-fix-the-secret","title":"Task 7: Fix the Secret","text":"<p>Open <code>practice_level_gp_appointments/config.py</code> and remove the hardcoded credentials. Replace them with a comment explaining the correct approach:</p> <pre><code># Credentials must come from environment variables, never hardcoded.\n# Set these in your shell or a .env file (which should be in .gitignore):\n#   export AWS_ACCESS_KEY_ID=\"your-key-here\"\n#   export AWS_SECRET_ACCESS_KEY=\"your-secret-here\"\n</code></pre>"},{"location":"workshops/pre-commit-hooks/#task-8-re-stage-and-commit-successfully","title":"Task 8: Re-stage and Commit Successfully","text":"<p>Stage all the changes \u2014 including the files <code>ruff</code> auto-fixed:</p> <pre><code>git add .\ngit commit -m \"add analysis notebook and config\"\n</code></pre> <p>This time, all hooks should pass and the commit should succeed:</p> <pre><code>gitleaks................................................................Passed\nruff (run with --fix)...................................................Passed\nruff-format.............................................................Passed\nnbstripout...............................................................Passed\n[main abc1234] add analysis notebook and config\n</code></pre> <p>Well done!</p> <p>You have set up three automated quality gates that will protect this repository on every future commit:</p> <ul> <li>No more accidental secret leaks</li> <li>Consistent code formatting, maintained automatically</li> <li>Clean notebook commits, always</li> </ul> <p>Note that pre-commit hooks run locally and handle formatting and secrets \u2014 they do not run your test suite. When you're ready, try the GitHub Actions workshop to run automated tests in CI and catch logic bugs that slip past local checks.</p>"},{"location":"workshops/pre-commit-hooks/#complete-file-reference","title":"Complete File Reference","text":"<p>If anything looks wrong in your config, compare against the complete version here.</p> <code>.pre-commit-config.yaml</code> \u2014 complete file <pre><code>repos:\n  - repo: https://github.com/gitleaks/gitleaks\n    rev: v8.18.4\n    hooks:\n      - id: gitleaks\n\n  - repo: https://github.com/astral-sh/ruff-pre-commit\n    rev: v0.4.0\n    hooks:\n      - id: ruff\n        args: [--fix]\n      - id: ruff-format\n\n  - repo: https://github.com/kynan/nbstripout\n    rev: 0.7.1\n    hooks:\n      - id: nbstripout\n</code></pre>"},{"location":"workshops/pre-commit-hooks/#going-further","title":"Going Further","text":"Running hooks manually <p>You can run all hooks against every file in the repository at any time \u2014 not just on staged files:</p> <pre><code>uv run pre-commit run --all-files\n</code></pre> <p>This is useful for applying hooks to a repository that already exists, or for checking that everything passes before a code review.</p> Updating hook versions <p>To update all hooks to their latest tagged versions:</p> <pre><code>uv run pre-commit autoupdate\n</code></pre> <p>This updates the <code>rev</code> fields in your <code>.pre-commit-config.yaml</code>. Always review the changes and test before committing.</p> Skipping a hook in an emergency <p>If you genuinely need to bypass a hook (for example, a known false-positive), you can skip it with:</p> <pre><code>git commit --no-verify -m \"my message\"\n</code></pre> <p>Use this sparingly. Pre-commit hooks only work as a safety net if you actually let them run.</p>"}]}